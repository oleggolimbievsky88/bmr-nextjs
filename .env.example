# Brand (bmr | controlfreak). Use same value for both.
# Favicon and site content follow this; set NEXT_PUBLIC_BRAND=controlfreak for Control Freak.
BRAND=bmr
NEXT_PUBLIC_BRAND=bmr

# Database - single URL (used in production; no MYSQL_* vars).
# For Vercel: use a reachable host (not localhost). Add ?ssl=true if required.
DATABASE_URL=mysql://user:password@host:3306/database

# Optional: shared brand config DB (same host/credentials, database=brand_core).
# When set, all brand reads/writes use this DB so multiple sites stay in sync.
# Run: mysql -u user -p < database/brand_core.sql
# DATABASE_BRAND_CORE_URL=mysql://user:password@host:3306/brand_core

# Optional: override logo/OG image per deployment
# NEXT_PUBLIC_HEADER_LOGO=/images/logo/...
# NEXT_PUBLIC_FOOTER_LOGO=/images/logo/...
# NEXT_PUBLIC_OG_IMAGE_URL=https://...
# NEXT_PUBLIC_SITE_URL=https://yoursite.com

# Static assets (e.g. Cloudflare R2). Used for vehicle thumbnails in nav, platform banners, shop-by-make.
# Set to your R2 public URL (e.g. https://assets.yoursite.com) so menu/platform images load from the bucket.
# NEXT_PUBLIC_ASSETS_BASE_URL=https://assets.yoursite.com
# Shop-by-make uploads: set this to the R2 public URL where uploads are actually served. If unset, ASSETS_BASE_URL is used.
# Use this when ASSETS_BASE_URL is the main site but R2 is on a different domain (fixes dead image links).
# NEXT_PUBLIC_UPLOADS_BASE_URL=https://assets.yoursite.com
# Optional: path under ASSETS_BASE_URL for vehicle thumbnails / platform headers.
# Defaults: siteart/cars, siteart/platformHeaders. For buckets without "siteart" use: cars, platformHeaders.
# NEXT_PUBLIC_PLATFORM_IMAGE_PATH=cars
# NEXT_PUBLIC_PLATFORM_BANNER_PATH=platformHeaders
# Optional: override full base URLs instead:
# NEXT_PUBLIC_PLATFORM_IMAGE_BASE_URL=https://assets.yoursite.com/siteart/cars
# NEXT_PUBLIC_PLATFORM_BANNER_BASE_URL=https://assets.yoursite.com/siteart/platformHeaders

# Product images: legacy (siteart/products) and admin uploads.
# NEXT_PUBLIC_PRODUCT_IMAGE_BASE_URL=https://yoursite.com/siteart/products
# Optional: base URL for admin-uploaded product images (p_*.jpg). Set in production when images
# live elsewhere (e.g. Vercel Blob CDN or dev server). Example: https://your-app.vercel.app
# NEXT_PUBLIC_ADMIN_PRODUCT_IMAGE_BASE_URL=https://your-app.vercel.app
# Install PDFs:
# NEXT_PUBLIC_INSTALL_BASE_URL=https://yoursite.com/siteart/install

# Admin product image uploads (Vercel Blob). Set in production so uploads store full URLs.
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...
