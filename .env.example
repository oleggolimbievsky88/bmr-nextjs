# Database Configuration
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=your_password_here
MYSQL_DATABASE=bmrsuspension

# Credit card token encryption (REQUIRED for checkout when saving card data)
# Generate: openssl rand -hex 32
# Add to .env.local and to Vercel/production env. Must be 64 hex characters.
CC_ENCRYPTION_KEY=your-encryption-key-here

# SEO / Site URL (for metadata, sitemap, robots, Open Graph)
# Production: https://www.bmrsuspension.com
NEXT_PUBLIC_SITE_URL=https://www.bmrsuspension.com

# Optional: OG/Twitter image URL (1200x630 recommended). Default: {SITE_URL}/images/bmr-logo.png
# NEXT_PUBLIC_OG_IMAGE_URL=https://www.bmrsuspension.com/og-image.png

# NextAuth (REQUIRED for login/session)
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

# PayPal checkout (from developer.paypal.com → Apps & Credentials)
# Sandbox: use Sandbox app credentials and PAYPAL_SANDBOX=true
# Live: use Live app credentials and PAYPAL_SANDBOX=false or omit
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-secret
PAYPAL_SANDBOX=true

# API URLs
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Platform/car images (siteart/cars). Default: {NEXT_PUBLIC_SITE_URL}/siteart/cars
# Use full URL if images 404 from mega menu. E.g. https://www.bmrsuspension.com/siteart/cars
# NEXT_PUBLIC_PLATFORM_IMAGE_BASE_URL=https://www.bmrsuspension.com/siteart/cars

# Cloudflare R2 (recommended for platform image uploads – bmrsuspension.com/siteart/* redirects here)
# Create R2 bucket, API token at Cloudflare dashboard → R2 → Manage R2 API Tokens
# R2_ACCOUNT_ID=your-account-id
# R2_ACCESS_KEY_ID=your-access-key
# R2_SECRET_ACCESS_KEY=your-secret-key
# R2_BUCKET_NAME=your-bucket-name
# R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com  (optional)